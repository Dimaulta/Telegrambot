# 📤 Инструкция по передаче .env файла с MacBook на VPS

## 🚀 Способ 1: SCP (рекомендуется)

### На MacBook выполни:

```bash
# Перейди в директорию проекта на MacBook
cd /path/to/Telegrambot

# Передай .env файл на сервер
scp config/.env root@85.208.110.226:/root/Telegrambot/config/.env
```

**Примечание:** Если используешь SSH ключ, команда выполнится автоматически. Если нет, тебе будет предложено ввести пароль root.

### После передачи на сервере:

Я автоматически установлю правильные права доступа, но если нужно вручную:

```bash
ssh root@85.208.110.226
chmod 600 /root/Telegrambot/config/.env
```

## 🔐 Способ 2: Зашифрованный архив (максимальная безопасность)

### На MacBook:

```bash
cd /path/to/Telegrambot

# Создай зашифрованный архив
tar -czf - config/.env | openssl enc -aes-256-cbc -salt -out /tmp/env.enc
# Введи пароль (запомни его!)

# Передай архив на сервер
scp /tmp/env.enc root@85.208.110.226:/tmp/

# Удали локальный архив
rm /tmp/env.enc
```

### На сервере:

```bash
ssh root@85.208.110.226
cd /root/Telegrambot
mkdir -p config
openssl enc -aes-256-cbc -d -in /tmp/env.enc | tar -xzf - -C .
chmod 600 config/.env
rm /tmp/env.enc
```

## ✏️ Способ 3: Вручную через SSH (если SCP не работает)

### На сервере:

```bash
ssh root@85.208.110.226
cd /root/Telegrambot
nano config/.env
```

Затем скопируй содержимое `.env` файла с MacBook и вставь в nano:
- Вставка: `Ctrl+Shift+V` (или правый клик → Вставить)
- Сохранение: `Ctrl+O`, затем `Enter`
- Выход: `Ctrl+X`

После этого:
```bash
chmod 600 config/.env
```

## ✅ Проверка после передачи

После передачи файла проверь:

```bash
# На сервере
ls -la /root/Telegrambot/config/.env
# Должно быть: -rw------- (только владелец может читать)

# Проверь что файл не пустой
wc -l /root/Telegrambot/config/.env
```

## 🔍 Важно!

1. **Убедись что `BASE_URL` в .env указывает на домен:**
   ```env
   BASE_URL=https://nowbots.ru
   ```
   (не IP адрес, иначе Let's Encrypt не выдаст сертификат)

2. **Проверь что все токены заполнены** для ботов, которые планируешь запускать

3. **Никогда не коммить .env в Git!**

