# Инструкция по деплою на Production сервер

Это руководство поможет тебе безопасно развернуть проект Telegram Bot Services на Linux сервере.

## 🔒 Безопасная передача .env файла

**⚠️ ВАЖНО: Никогда не коммить `.env` файл в Git!**

### Способ 1: SCP (рекомендуется)

1. **С твоего MacBook** передай `.env` файл на сервер через SCP:

```bash
# Замени user@server_ip на свои данные
scp config/.env user@your-server-ip:/path/to/Telegrambot/config/.env
```

2. **На сервере** проверь права доступа:

```bash
chmod 600 config/.env  # Только владелец может читать и писать
```

### Способ 2: Через SSH и nano/vim

1. **Подключись к серверу по SSH:**

```bash
ssh user@your-server-ip
```

2. **Создай файл на сервере:**

```bash
cd /path/to/Telegrambot
nano config/.env
```

3. **Вставь содержимое** из своего `.env` файла с MacBook (скопируй и вставь через терминал)

4. **Сохрани файл** (в nano: `Ctrl+O`, затем `Enter`, затем `Ctrl+X`)

5. **Установи правильные права:**

```bash
chmod 600 config/.env
```

### Способ 3: Через зашифрованный архив (для дополнительной безопасности)

1. **На MacBook** создай зашифрованный архив:

```bash
cd /path/to/Telegrambot
tar -czf - config/.env | openssl enc -aes-256-cbc -salt -out env.tar.gz.enc
# Тебе будет предложено ввести пароль — запомни его!
```

2. **Передай архив на сервер:**

```bash
scp env.tar.gz.enc user@your-server-ip:/tmp/
```

3. **На сервере** расшифруй архив:

```bash
ssh user@your-server-ip
cd /path/to/Telegrambot
mkdir -p config
openssl enc -aes-256-cbc -d -in /tmp/env.tar.gz.enc | tar -xzf - -C .
chmod 600 config/.env
rm /tmp/env.tar.gz.enc  # Удали архив после расшифровки
```

### Способ 4: Через переменные окружения (для небольших секретов)

Если нужно передать только несколько переменных, можешь использовать временный файл через SSH:

```bash
cat > /tmp/env_temp << 'EOF'
# Вставь здесь содержимое .env файла
BASE_URL=https://your-domain.com
VIDEO_BOT_TOKEN=your_token_here
...
EOF

# На сервере:
mv /tmp/env_temp /path/to/Telegrambot/config/.env
chmod 600 /path/to/Telegrambot/config/.env
```

### Проверка безопасности

После передачи `.env` файла:

1. **Проверь права доступа:**

```bash
ls -la config/.env
# Должно быть: -rw------- (только владелец может читать)
```

2. **Убедись, что файл в .gitignore:**

```bash
grep "\.env" .gitignore
# Должно быть: .env
```

3. **Проверь, что файл не попал в Git:**

```bash
git check-ignore config/.env
# Должно вернуть: config/.env
```

## 📋 Подготовка сервера

### Требования

- Linux сервер (Ubuntu/Debian рекомендуется)
- Docker и Docker Compose установлены
- Минимум 2GB RAM
- Минимум 10GB свободного места на диске
- Домен с настроенным DNS (для автоматического SSL через Let's Encrypt)
- Открытые порты 80 и 443 в firewall

### Установка Docker и Docker Compose

Если Docker еще не установлен:

```bash
# Обновляем систему
sudo apt update && sudo apt upgrade -y

# Устанавливаем Docker
curl -fsSL https://get.docker.com -o get-docker.sh
sudo sh get-docker.sh

# Добавляем текущего пользователя в группу docker
sudo usermod -aG docker $USER

# Выходим и заходим снова, чтобы изменения вступили в силу
exit
# (затем заново подключаемся по SSH)

# Устанавливаем Docker Compose
sudo curl -L "https://github.com/docker/compose/releases/latest/download/docker-compose-$(uname -s)-$(uname -m)" -o /usr/local/bin/docker-compose
sudo chmod +x /usr/local/bin/docker-compose

# Проверяем установку
docker --version
docker compose version
```

## 🚀 Процесс деплоя

### Шаг 1: Клонирование репозитория

```bash
# Если еще не клонировал
git clone https://github.com/Dimaulta/Telegrambot.git
cd Telegrambot
```

### Шаг 2: Передача .env файла

Используй один из способов выше для передачи `config/.env` на сервер.

### Шаг 3: Настройка BASE_URL и Traefik

1. **Открой `config/.env` и обнови `BASE_URL`:**

```env
BASE_URL=https://nowbots.ru
```

**Важно:** Используй свой реальный домен (не IP адрес) для работы с Let's Encrypt.

2. **Обнови email в Traefik конфигурации:**

Открой `config/traefik.yml` и замени email на свой:

```yaml
certificatesResolvers:
  letsencrypt:
    acme:
      email: admin@nowbots.ru  # ← Замени на свой email
```

3. **Проверь DNS настройки:**

Убедись, что домен `nowbots.ru` указывает на IP сервера:

```bash
dig nowbots.ru +short
# Должен вернуть IP твоего сервера
```

### Шаг 4: Создание необходимых директорий

```bash
mkdir -p Roundsvideobot/Resources/temporaryvideoFiles
mkdir -p Neurfotobot/tmp
mkdir -p config/certs
mkdir -p config/traefik/letsencrypt
```

### Шаг 5: Настройка firewall

Убедись, что порты 80 и 443 открыты:

```bash
sudo ufw allow 80/tcp
sudo ufw allow 443/tcp
sudo ufw status
```

**Важно:** Порт 80 нужен для получения SSL сертификата от Let's Encrypt (HTTP challenge).

### Шаг 6: Запуск деплоя

```bash
# Запускаем скрипт деплоя
./config/deploy.sh
```

Или вручную:

```bash
# Собираем образы
docker compose -f docker-compose.prod.yml build

# Запускаем сервисы
docker compose -f docker-compose.prod.yml up -d

# Проверяем статус
docker compose -f docker-compose.prod.yml ps
```

### Шаг 7: Настройка webhooks

После успешного запуска сервисов, настрой webhooks для всех ботов:

```bash
# Загружаем переменные окружения
set -a; source config/.env; set +a

# Запускаем скрипт настройки webhooks
./config/set-webhooks.sh
```

Или настрой вручную через API Telegram, используя URL вида:
- `https://nowbots.ru/neurfoto/webhook` для Neurfotobot
- `https://nowbots.ru/nowmtt/webhook` для NowmttBot
- `https://nowbots.ru/rounds/webhook` для Roundsvideobot
- `https://nowbots.ru/gs/text/webhook` для GSForTextBot
- `https://nowbots.ru/contentfabrika/webhook` для ContentFabrikaBot
- `https://nowbots.ru/pereskaznow/webhook` для PereskazNowBot
- `https://nowbots.ru/nowcontroller/webhook` для NowControllerBot

**Важно:** Все webhook'и должны использовать HTTPS (не HTTP) для работы с Telegram API.

## 🔍 Проверка работы

### Просмотр логов

```bash
# Все сервисы
docker compose -f docker-compose.prod.yml logs -f

# Конкретный сервис
docker compose -f docker-compose.prod.yml logs -f nowcontrollerbot
```

### Проверка статуса контейнеров

```bash
docker compose -f docker-compose.prod.yml ps
```

### Проверка health check

```bash
# Через HTTPS (после получения SSL сертификата)
curl https://nowbots.ru/neurfoto/webhook -I

# Или локально через Traefik
curl http://localhost:8084/health
```

### Проверка SSL сертификата

```bash
# Проверь что SSL сертификат получен (может занять 1-2 минуты)
curl -I https://nowbots.ru/neurfoto/webhook

# Проверь детали сертификата
openssl s_client -connect nowbots.ru:443 -servername nowbots.ru < /dev/null
```

**Примечание:** Traefik автоматически получает SSL сертификат при первом запуске. Это может занять 1-2 минуты.

## 🔄 Обновление проекта

При обновлении кода:

```bash
# Обновляем код
git pull

# Пересобираем и перезапускаем
docker compose -f docker-compose.prod.yml down
docker compose -f docker-compose.prod.yml build --no-cache
docker compose -f docker-compose.prod.yml up -d
```

## 🛑 Остановка сервисов

```bash
# Остановить все сервисы
docker compose -f docker-compose.prod.yml down

# Остановить с удалением volumes (осторожно — удалит данные БД!)
docker compose -f docker-compose.prod.yml down -v
```

## 🐛 Решение проблем

### Сервис не запускается

1. Проверь логи:
```bash
docker compose -f docker-compose.prod.yml logs service_name
```

2. Проверь, что `.env` файл заполнен правильно

3. Проверь, что все необходимые токены установлены

### Проблемы с портами

Если порты уже заняты, измени их в `docker-compose.prod.yml`:

```yaml
ports:
  - "8084:8084"  # Измени внешний порт (левая часть)
```

### Проблемы с правами доступа

```bash
# Убедись, что у пользователя есть права на Docker
sudo usermod -aG docker $USER
# Выйди и зайди снова

# Проверь права на .env файл
chmod 600 config/.env
```

## 📝 Дополнительные настройки

### Автозапуск при перезагрузке сервера

Docker Compose с `restart: unless-stopped` автоматически перезапустит контейнеры при перезагрузке сервера.

### Мониторинг

Рекомендуется использовать мониторинг для отслеживания состояния сервисов:
- [Portainer](https://www.portainer.io/) — веб-интерфейс для Docker
- [Prometheus + Grafana](https://prometheus.io/) — для метрик
- Простые скрипты проверки health endpoints

### Бэкапы

Регулярно делай бэкапы:
- БД: `config/monetization.sqlite`, `contentfabrikabot/db.sqlite`
- Файлы: временные директории (если нужно сохранить данные)

```bash
# Пример бэкапа
tar -czf backup-$(date +%Y%m%d).tar.gz config/*.sqlite contentfabrikabot/db.sqlite
```

## 🔐 Безопасность

1. **HTTPS настроен автоматически** через Traefik и Let's Encrypt
2. **Не храни `.env` в Git**
3. **Регулярно обновляй токены ботов**
4. **Используй firewall** для ограничения доступа к портам (только 80, 443, 22)
5. **Регулярно обновляй Docker образы** для безопасности
6. **Измени пароль Traefik Dashboard** (см. `docs/TRAEFIK_SETUP.md`)

## 📚 Дополнительная документация

- **Настройка Traefik**: `docs/TRAEFIK_SETUP.md` — подробная инструкция по Traefik и SSL
- **Безопасная передача .env**: `docs/ENV_SECURITY.md` — способы передачи секретов

## 📞 Поддержка

При возникновении проблем:
1. Проверь логи сервисов
2. Убедись, что все зависимости установлены
3. Проверь документацию в папке `docs/`
