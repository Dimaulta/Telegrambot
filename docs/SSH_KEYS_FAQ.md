# 🔐 FAQ: SSH ключи и срок годности

## ❓ У SSH ключей есть срок годности?

**НЕТ! SSH ключи не имеют срока годности.** Они работают до тех пор, пока:

1. ✅ Приватный ключ не удалён с твоего устройства
2. ✅ Публичный ключ не удалён из `~/.ssh/authorized_keys` на сервере
3. ✅ Сервер поддерживает алгоритм шифрования ключа (ED25519 поддерживается уже много лет)

## 🔒 Как долго работают SSH ключи?

**Бессрочно!** Твой текущий ключ (ED25519) будет работать столько, сколько тебе нужно:

- ED25519 - современный и безопасный алгоритм
- Используется уже более 10 лет
- Не планируется его устаревание в ближайшие десятилетия
- Даже старые алгоритмы (RSA, DSA) работают годами

## ⚠️ Когда ключ перестанет работать?

### 1. **Удаление ключа**
- Если случайно удалишь приватный ключ с устройства
- Если удалишь публичный ключ из `authorized_keys` на сервере

### 2. **Изменение прав доступа**
- Если права на `~/.ssh/authorized_keys` станут неправильными (должно быть 600)
- Если права на `~/.ssh/` станут неправильными (должно быть 700)

### 3. **Изменение конфигурации SSH**
- Если отключишь `PubkeyAuthentication` в `/etc/ssh/sshd_config`
- Если изменишь путь к `AuthorizedKeysFile`

### 4. **Очень редко: устаревание алгоритма**
- Это происходит крайне редко (раз в 10-20 лет)
- Твой алгоритм ED25519 - современный, не устареет долго
- Если вдруг устареет - у тебя будет много времени на миграцию

## 💡 Рекомендации по безопасности

### ✅ Что делать:

1. **Сделай резервную копию приватного ключа:**
   ```bash
   # На своем устройстве (НЕ на сервере!)
   cp ~/.ssh/id_ed25519 ~/.ssh/id_ed25519.backup
   # Или сохрани в password manager (1Password, LastPass, Bitwarden)
   ```

2. **Добавь несколько ключей:**
   - Один ключ для основного устройства
   - Второй ключ для запасного устройства
   - Третий ключ как резервный (можно хранить в безопасном месте)

3. **Регулярно проверяй ключи:**
   ```bash
   # Проверить, какие ключи есть на сервере
   cat ~/.ssh/authorized_keys
   
   # Проверить отпечатки ключей
   ssh-keygen -l -f ~/.ssh/authorized_keys
   ```

4. **Веди список ключей:**
   - Записывай, для какого устройства какой ключ
   - Сохрани отпечатки ключей (fingerprint) в безопасном месте

### ❌ Чего НЕ делать:

- ❌ Не коммить приватные ключи в Git
- ❌ Не отправлять приватные ключи по email
- ❌ Не хранить приватные ключи в незашифрованном виде на публичных серверах
- ❌ Не делиться приватными ключами с кем-либо

## 🔄 Обновление ключей (опционально)

**Вопрос:** Нужно ли периодически менять SSH ключи?

**Ответ:** Не обязательно, но можно:

### Когда стоит обновить ключ:
- Если подозреваешь, что ключ скомпрометирован
- Если устройство было утеряно или украдено
- Если хочешь использовать более новый алгоритм (но ED25519 уже отличный)

### Как обновить ключ:

1. **Создай новый ключ:**
   ```bash
   ssh-keygen -t ed25519 -C "новый-ключ-2025" -f ~/.ssh/vps_new
   ```

2. **Добавь новый ключ на сервер** (пока старый работает):
   ```bash
   ssh root@nowbots.ru
   cat ~/.ssh/vps_new.pub >> ~/.ssh/authorized_keys
   ```

3. **Протестируй новый ключ:**
   ```bash
   ssh -i ~/.ssh/vps_new root@nowbots.ru
   ```

4. **Удали старый ключ** (опционально):
   ```bash
   ssh root@nowbots.ru
   nano ~/.ssh/authorized_keys
   # Удали строку со старым ключом
   ```

## 🚨 Что делать, если потерял доступ?

### Сценарий 1: Удалил приватный ключ с устройства
**Решение:**
- Восстанови из резервной копии
- Используй другой ключ (если добавлял несколько)
- Используй панель управления VPS для доступа через веб-консоль

### Сценарий 2: Сервер больше не принимает ключи
**Решение:**
- Подключись через веб-консоль панели управления
- Проверь права на `~/.ssh/authorized_keys` (должно быть 600)
- Проверь права на `~/.ssh/` (должно быть 700)
- Проверь конфигурацию SSH: `grep PubkeyAuthentication /etc/ssh/sshd_config`

### Сценарий 3: Забыл, какой ключ использовать
**Решение:**
- Посмотри отпечатки ключей на сервере: `ssh-keygen -l -f ~/.ssh/authorized_keys`
- Сравни с отпечатками на устройстве: `ssh-keygen -l -f ~/.ssh/id_ed25519.pub`

## 📊 Текущее состояние твоих ключей

**На сервере сейчас:**
- 1 SSH ключ (ED25519)
- Отпечаток: `SHA256:sSLA0kiAEU/Yd/wnsl0439oLnH1c/QmoJfTEydl8Ovc`
- Права настроены правильно
- Ключ работает стабильно

**Рекомендация:** Добавь хотя бы один резервный ключ для безопасности!

## ✅ Вывод

**SSH ключи работают бессрочно!** Не переживай о сроке годности. Главное:

1. ✅ Храни резервную копию приватного ключа
2. ✅ Добавь несколько ключей для надёжности
3. ✅ Регулярно проверяй, что ключи работают
4. ✅ Веди список ключей и устройств

Твой текущий ключ будет работать столько, сколько нужно! 🎉

