# ContentFabrikaBot — план установки и запуска

Документ описывает, как подготовить окружение, получить все ключи и запустить ContentFabrikaBot — бота, который изучает стиль канала и генерирует тексты для ручной публикации.

## 1. Что потребуется

### Аккаунты и ключи
- **Telegram Bot** — создаём через `@BotFather`, сохраняем токен (`CONTENTFABRIKABOT_TOKEN`).
- **OpenAI** — нужен действующий `OPENAI_API_KEY` с доступом к выбранной модели (по умолчанию `gpt-4o-mini`).

> Всё про окружение, проксирование, ngrok и nginx уже разобрано в `docs/SETUP_GUIDE.md`. Здесь оставлены только шаги, специфичные для ContentFabrikaBot.

### Модель генерации

- Модель задаётся переменной окружения **`OPENAI_MODEL`**. Если не задана — используется **`gpt-4o-mini`**.
- Тарифы (Standard, за 1M токенов): см. [официальный прайс](https://platform.openai.com/docs/pricing). Для gpt-4o-mini: **$0.15 вход / $0.60 выход**.

## 2. Настройка `.env`

1. Создай файл окружения:
   ```bash
   cp docs/env.example config/.env
   ```
2. Заполни обязательные переменные:
   ```env
   CONTENTFABRIKABOT_TOKEN=123456:ABCDEF...
   OPENAI_API_KEY=sk-live-...
   ```
   - `CONTENTFABRIKABOT_TOKEN` — токен из `@BotFather`.
   - `OPENAI_API_KEY` — ключ OpenAI с активным биллингом. Один и тот же ключ работает для всех моделей Chat Completions (gpt-4o-mini, gpt-4.1-nano, gpt-5-mini и др.).
   - `OPENAI_MODEL` (необязательно) — имя модели для Chat Completions. По умолчанию `gpt-4o-mini`. После отключения gpt-4o-mini можно указать, например, `gpt-4.1-nano`, `gpt-4.1-mini` или `gpt-5-mini` (см. раздел про миграцию ниже).
3. При необходимости добавь `LOG_LEVEL=debug`, чтобы видеть подробные логи обработки вебхуков.

## 3. Проверка ключей OpenAI

Перед запуском убедись, что ключ рабочий:
```bash
curl https://api.openai.com/v1/models \
  -H "Authorization: Bearer $OPENAI_API_KEY"
```
В ответе должен быть список моделей. Ошибка 401 означает неверный ключ или неактивный биллинг.

## 4. Установка webhook Telegram

Когда публичный URL готов:
```bash
source config/.env
curl -X POST "https://api.telegram.org/bot${CONTENTFABRIKABOT_TOKEN}/setWebhook" \
  -H "Content-Type: application/json" \
  -d "{\"url\":\"${BASE_URL}/contentfabrika/webhook\"}"

curl "https://api.telegram.org/bot${CONTENTFABRIKABOT_TOKEN}/getWebhookInfo"
```
- В `getWebhookInfo` поле `url` должно совпадать с `${BASE_URL}/contentfabrika/webhook`.
- `pending_update_count` → 0 (или небольшое число во время тестов).

## 5. Локальный запуск

```bash
cd /Users/<user>/Desktop/projects/Telegrambot
set -a; source config/.env; set +a
swift run ContentFabrikaBot
```
- При первом запуске автоматически создаётся `contentfabrikabot/db.sqlite` и применяются миграции.
- В логах увидишь загрузку `.env`, старт сервера и входящие вебхуки.
- Для остановки используй `Ctrl + C`.

## 6. Сценарий проверки

1. В Telegram отправь боту `/start` — он пришлёт приветствие и объяснит новый поток (Forward → анализ → генерация текста).
2. Перешли от 3 до 10 постов из своего канала (функция Forward). После третьего появится кнопка «📚 Изучить канал».
3. Нажми кнопку — бот сформирует профиль стиля через OpenAI.
4. Отправь тему/промт. Ты получишь готовый текст без медиа. Бот отдельно напомнит скопировать текст и опубликовать его вручную.

## 7. Типичные проблемы

| Симптом | Как поправить |
| --- | --- |
| `config/.env not found while configuring ContentFabrikaBot` | Убедись, что файл создан и запущена команда `set -a; source config/.env; set +a` |
| `OpenAI API error 401/429` | Проверь ключ и лимиты в кабинете OpenAI |
| Webhook не приходит | Следуй разделу про инфраструктуру в `docs/SETUP_GUIDE.md`, проверь `BASE_URL` и `getWebhookInfo` |
| Кнопки «Изучить канал» нет | Нужно минимум 3 пересланных поста; бот хранит их в SQLite |
| Ошибка «Слишком много запросов» | Срабатывает встроенный rate-limit (не больше 2 генераций в минуту на пользователя) |

## 8. Миграция с gpt-4o-mini (февраль 2026)

OpenAI отключает **gpt-4o-mini** в ChatGPT 13 февраля 2026; в API модель может оставаться доступной дольше, но лучше заранее перейти на другую модель. **Новый ключ не нужен** — тот же `OPENAI_API_KEY` используется для всех моделей Chat Completions.

### Варианты замены (тот же API, Chat Completions)

| Модель        | Вход (1M токенов) | Выход (1M токенов) | Комментарий |
|---------------|-------------------|---------------------|-------------|
| **gpt-4o-mini** (текущая) | $0.15 | $0.60 | Может быть отключена в 2026. |
| **gpt-4.1-nano**         | $0.10 | $0.40 | Дешевле по входу, сопоставимый выход; подходит для экономии. |
| **gpt-4.1-mini**         | $0.40 | $1.60 | Ближе по качеству к gpt-4o-mini, дороже. |
| **gpt-5-mini**           | $0.25 | $2.00 | «Быстрая и дешёвая» версия GPT-5 для чётких задач. |
| **gpt-5-nano**           | $0.05 | $0.40 | Самый дешёвый вариант; качество стоит проверить на своих сценариях. |

Цены — Standard tier, [platform.openai.com/docs/pricing](https://platform.openai.com/docs/pricing).

### Рекомендация по стоимости

- **Минимальный бюджет:** `gpt-4.1-nano` или `gpt-5-nano` — дешевле или сопоставимо с gpt-4o-mini.
- **Баланс цена/качество:** `gpt-4.1-mini` — если важна стабильность качества анализа стиля и генерации.

### План обновления

1. **Уже сделано в коде:** модель читается из `OPENAI_MODEL`; если не задана — используется `gpt-4o-mini`.
2. **До 13 февраля 2026:** в `config/.env` добавь, например:
   ```env
   OPENAI_MODEL=gpt-4.1-mini
   ```
   или `gpt-4.1-nano` / `gpt-5-mini` / `gpt-5-nano` по желанию.
3. **Проверка:** перезапусти бота, сделай «Изучить канал» и генерацию поста — убедись, что качество устраивает.
4. **После отключения gpt-4o-mini:** если переменную не задавал, бот начнёт получать ошибки от API; задай одну из моделей выше в `OPENAI_MODEL`.

Бот готов к работе после выполнения шагов выше. Все тексты отправляются только в личный чат с автором — публикация в канал происходит вручную.  

