# ✅ Чеклист бэкапа перед отключением пароля

## 📋 Что нужно сделать

### 1. 🖥️ Бэкап ПРИВАТНОГО ключа на локальном устройстве

**ВАЖНО:** Делай это на СВОЁМ устройстве (Mac/Windows), НЕ на сервере!

**Шаги:**

```bash
# На локальном устройстве (Mac/Linux):
cd ~/.ssh
tar -czf ~/ssh_private_key_backup_$(date +%Y%m%d).tar.gz id_ed25519 id_ed25519.pub

# Сохрани в безопасное место:
# - Password manager (1Password, LastPass, Bitwarden)
# - Зашифрованное облако (iCloud Keychain, зашифрованный Dropbox)
# - Внешний диск (флешка)
# - Два разных места для надёжности!
```

**Что бэкапить:**
- ✅ `id_ed25519` - приватный ключ (САМОЕ ВАЖНОЕ!)
- ✅ `id_ed25519.pub` - публичный ключ (для справки)
- ✅ SSH config - если есть настройки

**⚠️ НЕ отправляй приватный ключ на сервер!** Он должен оставаться только на твоём устройстве.

---

### 2. 🖧 Бэкап authorized_keys на сервере

**Что это:** Файл с публичными ключами всех устройств, которым разрешён доступ.

**Безопасность:** ✅ **ПОЛНОСТЬЮ БЕЗОПАСНО!** Это только публичные ключи, их можно передавать.

**Шаги:**

```bash
# На сервере (через SSH):
ssh root@nowbots.ru

# Создай бэкап
cp ~/.ssh/authorized_keys ~/.ssh/authorized_keys.backup_$(date +%Y%m%d)

# Скопируй на локальное устройство (БЕЗОПАСНО!)
# Выполни с ЛОКАЛЬНОГО устройства:
scp root@nowbots.ru:/root/.ssh/authorized_keys.backup_* ~/Downloads/
```

**Или сделай бэкап всей директории .ssh на сервере:**
```bash
# На сервере:
tar -czf ~/ssh_server_backup_$(date +%Y%m%d).tar.gz ~/.ssh/

# Скопируй на локальное устройство:
scp root@nowbots.ru:/root/ssh_server_backup_*.tar.gz ~/Downloads/
```

**Почему безопасно:**
- В `authorized_keys` только публичные ключи
- Публичные ключи можно показывать всем
- Они не дают доступа без приватного ключа
- Можно безопасно передавать по scp, email, сохранять в облаке

---

### 3. 💾 Сохранение бэкапов локально

**Рекомендуемая структура:**

```
~/Backups/SSH/
├── private_key_20251224.tar.gz        ← Приватный ключ (ЗАШИФРОВАН!)
├── authorized_keys_20251224.txt       ← Публичные ключи с сервера
└── ssh_info.txt                       ← Отпечатки ключей, даты, заметки
```

**Создай файл с информацией:**
```bash
# На локальном устройстве:
cat > ~/Backups/SSH/ssh_info.txt <<EOF
Дата бэкапа: $(date)
Сервер: nowbots.ru

Отпечаток ключа:
$(ssh-keygen -l -f ~/.ssh/id_ed25519.pub 2>/dev/null || echo "Запусти на устройстве с ключом")

Расположение бэкапов:
- Приватный ключ: ~/Backups/SSH/private_key_*.tar.gz
- authorized_keys: ~/Backups/SSH/authorized_keys_*.txt

Важно:
- Приватный ключ храни только в зашифрованном виде
- authorized_keys можно хранить где угодно (это публичные ключи)
EOF
```

---

## 🔒 Безопасность передачи

### ✅ Безопасно передавать:

1. **authorized_keys** (публичные ключи):
   - ✅ Через scp (ssh копирование)
   - ✅ По email
   - ✅ В облако (Dropbox, Google Drive, iCloud)
   - ✅ В Git репозиторий (хотя не обязательно)
   - ✅ В password manager

2. **Публичный ключ** (`id_ed25519.pub`):
   - ✅ Можно показывать всем
   - ✅ Можно загружать на GitHub, GitLab и т.д.

### ❌ НИКОГДА не передавай:

1. **Приватный ключ** (`id_ed25519`):
   - ❌ НЕ отправляй по email
   - ❌ НЕ загружай на публичные серверы
   - ❌ НЕ коммить в Git
   - ❌ НЕ отправляй на сервер через scp
   - ✅ Только: password manager, зашифрованное хранилище, физически защищённое место

---

## 📝 Пошаговый чеклист

### Перед отключением пароля:

- [ ] **Шаг 1:** Создал бэкап приватного ключа на локальном устройстве
- [ ] **Шаг 2:** Сохранил бэкап приватного ключа в 2-3 безопасных места
- [ ] **Шаг 3:** Создал бэкап `authorized_keys` на сервере
- [ ] **Шаг 4:** Скачал бэкап `authorized_keys` на локальное устройство
- [ ] **Шаг 5:** Проверил, что приватный ключ работает (подключение без пароля)
- [ ] **Шаг 6:** Записал отпечаток ключа (fingerprint) для справки
- [ ] **Шаг 7:** Готов отключить пароль! ✅

---

## 🎯 Краткая инструкция

### Прямо сейчас:

**1. На локальном устройстве:**
```bash
cd ~/.ssh
tar -czf ~/ssh_private_backup_$(date +%Y%m%d).tar.gz id_ed25519 id_ed25519.pub
# Сохрани в password manager или зашифрованное хранилище
```

**2. На сервере (подключись по SSH):**
```bash
cp ~/.ssh/authorized_keys ~/.ssh/authorized_keys.backup_$(date +%Y%m%d)
```

**3. Скачай authorized_keys на локальное устройство:**
```bash
# Выполни с локального устройства:
scp root@nowbots.ru:/root/.ssh/authorized_keys.backup_* ~/Downloads/
```

**4. Проверь отпечаток ключа (для справки):**
```bash
# На локальном устройстве:
ssh-keygen -l -f ~/.ssh/id_ed25519.pub
# Запиши куда-нибудь для справки
```

**Готово!** Теперь можно безопасно отключать пароль! 🎉

---

## 🚨 Важные напоминания

1. **Приватный ключ** - храни только в зашифрованном виде и в безопасных местах
2. **authorized_keys** - можно передавать безопасно, это только публичные ключи
3. **Храни бэкапы в нескольких местах** - не храни всё в одном месте
4. **Регулярно проверяй бэкапы** - убедись, что можешь восстановить доступ

---

## ✅ Итоговый ответ на твой вопрос

**Что нужно сделать:**
1. ✅ Бэкап приватного ключа на локальном устройстве - ДА
2. ✅ Бэкап authorized_keys на сервере - ДА
3. ✅ Сохранить authorized_keys локально - ДА

**Безопасность передачи authorized_keys:**
- ✅ **ПОЛНОСТЬЮ БЕЗОПАСНО!** Это только публичные ключи
- ✅ Можно передавать через scp, email, облако
- ✅ Они не дают доступа без приватного ключа

**Безопасность приватного ключа:**
- ⚠️ **НИКОГДА не передавай приватный ключ на сервер!**
- ✅ Храни только локально в зашифрованном виде
- ✅ Используй password manager или зашифрованное хранилище

