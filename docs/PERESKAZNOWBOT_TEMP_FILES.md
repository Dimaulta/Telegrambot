# 📁 Временные файлы PereskazNowBot в Docker

## 🔍 Как работает PereskazNowBot

PereskazNowBot скачивает аудио с YouTube через `yt-dlp`, обрабатывает его через `ffmpeg` (если нужно сжать), и отправляет в OpenAI Whisper API для транскрипции.

## 📂 Где сохраняются временные файлы

### В Docker контейнере:

1. **Временная директория**: `/tmp` (системная временная директория контейнера)
   - Код использует `FileManager.default.temporaryDirectory`
   - В Linux контейнере это будет `/tmp`

2. **Структура папок**:
   ```
   /tmp/
     └── {UUID}/              # Уникальная папка для каждого запроса
         ├── audio.m4a        # Скачанное аудио (5-50 MB)
         ├── audio_compressed.m4a  # Сжатая версия (если > 25MB)
         └── audio_compressed2.m4a # Вторая попытка сжатия (если нужно)
   ```

3. **Автоматическое удаление**:
   - Код использует `defer` блок который удаляет всю папку после обработки
   - Удаляется через `FileManager.default.removeItem(at: workDir)`
   - Промежуточные файлы yt-dlp удаляются самим yt-dlp

## ✅ Что было настроено

### 1. Установка зависимостей в Dockerfile.prod:

```dockerfile
# Установлены:
- ffmpeg (для сжатия аудио)
- python3, python3-pip (для yt-dlp)
- yt-dlp (скачивается как бинарник в /usr/local/bin/yt-dlp)
```

### 2. Конфигурация в docker-compose.prod.yml:

```yaml
pereskaznowbot:
  # НЕТ volume для временных файлов
  # Временные файлы хранятся в /tmp контейнера
  # Автоматически удаляются после обработки
```

## 🎯 Почему не нужен volume для временных файлов

1. **Автоматическое удаление**: Код удаляет файлы через `defer` блок
2. **Изоляция**: Каждый запрос создает свою папку с UUID
3. **Временность**: Файлы нужны только на время обработки (несколько минут)
4. **Размер**: Обычно 5-50 MB, не критично для /tmp контейнера

## ⚠️ Важные моменты

### Размер временных файлов:

- **Обычный размер**: 5-50 MB (зависит от длительности видео)
- **Лимит Whisper API**: 25 MB
- **Автоматическое сжатие**: Если файл > 25MB, используется ffmpeg для сжатия

### Очистка:

- **Автоматическая**: Файлы удаляются после обработки
- **При ошибках**: Если обработка прервется, файлы могут остаться
- **Мониторинг**: Можно проверить `/tmp` в контейнере если нужно

## 🔧 Если нужно добавить volume (не рекомендуется)

Если по какой-то причине нужно сохранять временные файлы на хосте:

```yaml
pereskaznowbot:
  volumes:
    - ./pereskaznowbot/tmp:/tmp/pereskaznowbot
```

**Но это НЕ рекомендуется**, так как:
- Файлы должны удаляться автоматически
- Накопление файлов на диске нежелательно
- Код уже правильно обрабатывает удаление

## 📊 Мониторинг использования диска

Если нужно проверить использование временных файлов:

```bash
# Проверить размер /tmp в контейнере
docker exec telegrambot_pereskaznowbot du -sh /tmp

# Проверить количество файлов
docker exec telegrambot_pereskaznowbot find /tmp -type f | wc -l

# Очистить /tmp вручную (если нужно)
docker exec telegrambot_pereskaznowbot find /tmp -type f -delete
```

## ✅ Итог

- ✅ Временные файлы хранятся в `/tmp` контейнера
- ✅ Автоматически удаляются после обработки
- ✅ Не накапливаются на диске
- ✅ yt-dlp и ffmpeg установлены в Dockerfile
- ✅ Не требуется дополнительная настройка volumes

Всё готово для работы! 🚀

