# 🔐 Полное руководство по SSH: настройка, бэкап и FAQ

## 📋 Содержание

1. [Настройка SSH ключей](#настройка-ssh-ключей)
2. [Бэкап ключей](#бэкап-ключей)
3. [FAQ и решение проблем](#faq-и-решение-проблем)
4. [Объяснение сообщений SSH](#объяснение-сообщений-ssh)

---

## 🔑 Настройка SSH ключей

### Как добавить новое устройство для подключения

#### Способ 1: Создание SSH ключа на новом устройстве (РЕКОМЕНДУЕТСЯ)

**Шаг 1: Создай SSH ключ на своем устройстве (Mac/Windows/Linux)**

**На Mac/Linux:**
```bash
ssh-keygen -t ed25519 -C "название-устройства" -f ~/.ssh/vps_ключ
# Например: ssh-keygen -t ed25519 -C "мой-macbook" -f ~/.ssh/vps_macbook
```

**На Windows (PowerShell или Git Bash):**
```bash
ssh-keygen -t ed25519 -C "название-устройства" -f ~/.ssh/vps_ключ
```

Система попросит ввести пароль для ключа (опционально, но рекомендуется). Если не хочешь пароль - просто нажми Enter дважды.

**Шаг 2: Скопируй публичный ключ**

**На Mac/Linux:**
```bash
cat ~/.ssh/vps_ключ.pub
```

**На Windows (PowerShell):**
```bash
type ~\.ssh\vps_ключ.pub
```

Скопируй весь вывод (начинается с `ssh-ed25519 ...`)

**Шаг 3: Добавь ключ на сервер**

**ВАЖНО:** Выполняй эти команды, пока у тебя еще есть доступ по SSH (с паролем или старым ключом):

```bash
# Подключись к серверу
ssh root@nowbots.ru

# Добавь новый ключ в authorized_keys
echo "ssh-ed25519 AAAAC3... твой_публичный_ключ_здесь название-устройства" >> ~/.ssh/authorized_keys

# Установи правильные права
chmod 600 ~/.ssh/authorized_keys
chmod 700 ~/.ssh
```

**Шаг 4: Протестируй подключение с нового устройства**

**На Mac/Linux:**
```bash
ssh -i ~/.ssh/vps_ключ root@nowbots.ru
```

**На Windows:**
```bash
ssh -i ~/.ssh/vps_ключ root@nowbots.ru
```

Если всё работает - можешь добавить в SSH config для удобства:

**~/.ssh/config (Mac/Linux) или ~/.ssh/config (Windows):**
```
Host nowbots
    HostName nowbots.ru
    User root
    Port 2222
    IdentityFile ~/.ssh/vps_ключ
```

После этого можно просто: `ssh nowbots`

---

### Способ 2: Использование существующего ключа (если уже есть)

Если у тебя уже есть SSH ключ на устройстве (например, `~/.ssh/id_rsa.pub` или `~/.ssh/id_ed25519.pub`):

1. Скопируй публичный ключ:
```bash
cat ~/.ssh/id_ed25519.pub  # или id_rsa.pub
```

2. Добавь на сервер (пока есть доступ):
```bash
ssh root@nowbots.ru
echo "твой_публичный_ключ_здесь" >> ~/.ssh/authorized_keys
chmod 600 ~/.ssh/authorized_keys
```

---

### Способ 3: Использование ssh-copy-id (самый простой, если доступен)

**На Mac/Linux:**
```bash
ssh-copy-id -i ~/.ssh/vps_ключ.pub root@nowbots.ru
```

Эта команда автоматически скопирует ключ и установит правильные права.

---

## 💾 Бэкап ключей

### Где хранятся SSH ключи

**На локальном устройстве (Mac/Windows/Linux):**
- Стандартная директория: `~/.ssh/` (в домашней папке)
- Приватный ключ: `~/.ssh/id_ed25519` (СЕКРЕТНЫЙ! Никому не показывай!)
- Публичный ключ: `~/.ssh/id_ed25519.pub` (можно показывать)

**На сервере (VPS):**
- Директория: `/root/.ssh/`
- Файл: `authorized_keys` - содержит публичные ключи всех устройств

**ВАЖНО:** На сервере хранятся только ПУБЛИЧНЫЕ ключи. Приватные ключи на сервере НЕ хранятся!

---

### Как сделать бэкап

#### Бэкап приватного ключа на локальном устройстве

**Mac/Linux:**
```bash
# Перейди в директорию с ключами
cd ~/.ssh

# Создай папку для бэкапа
mkdir -p ~/ssh_backup_$(date +%Y%m%d)

# Скопируй все ключи
cp id_ed25519 ~/ssh_backup_$(date +%Y%m%d)/
cp id_ed25519.pub ~/ssh_backup_$(date +%Y%m%d)/
cp config ~/ssh_backup_$(date +%Y%m%d)/ 2>/dev/null || true

# Создай архив
cd ~
tar -czf ssh_backup_$(date +%Y%m%d).tar.gz ssh_backup_$(date +%Y%m%d)/

# Удали временную папку
rm -rf ssh_backup_$(date +%Y%m%d)/
```

**Windows (PowerShell):**
```powershell
# Создай папку для бэкапа
$backupDir = "$env:USERPROFILE\ssh_backup_$(Get-Date -Format 'yyyyMMdd')"
New-Item -ItemType Directory -Path $backupDir -Force

# Скопируй ключи
Copy-Item "$env:USERPROFILE\.ssh\id_ed25519" -Destination $backupDir
Copy-Item "$env:USERPROFILE\.ssh\id_ed25519.pub" -Destination $backupDir
Copy-Item "$env:USERPROFILE\.ssh\config" -Destination $backupDir -ErrorAction SilentlyContinue
```

**Сохрани бэкап в безопасное место:**
- Password manager (1Password, LastPass, Bitwarden)
- Зашифрованное облако (iCloud Keychain, зашифрованный Dropbox)
- Внешний диск (флешка)
- Два разных места для надёжности!

---

#### Бэкап authorized_keys на сервере

**ВАЖНО:** Это только публичные ключи, их можно передавать безопасно!

```bash
# Подключись к серверу
ssh root@nowbots.ru

# Создай бэкап
cp ~/.ssh/authorized_keys ~/.ssh/authorized_keys.backup_$(date +%Y%m%d)

# Скопируй на локальное устройство (БЕЗОПАСНО!)
# Выполни с ЛОКАЛЬНОГО устройства:
scp root@nowbots.ru:/root/.ssh/authorized_keys.backup_* ~/Downloads/
```

---

### Чеклист бэкапа перед отключением пароля

**Перед отключением пароля:**

- [ ] **Шаг 1:** Создал бэкап приватного ключа на локальном устройстве
- [ ] **Шаг 2:** Сохранил бэкап приватного ключа в 2-3 безопасных места
- [ ] **Шаг 3:** Создал бэкап `authorized_keys` на сервере
- [ ] **Шаг 4:** Скачал бэкап `authorized_keys` на локальное устройство
- [ ] **Шаг 5:** Проверил, что приватный ключ работает (подключение без пароля)
- [ ] **Шаг 6:** Записал отпечаток ключа (fingerprint) для справки
- [ ] **Шаг 7:** Готов отключить пароль! ✅

---

## ❓ FAQ и решение проблем

### У SSH ключей есть срок годности?

**НЕТ! SSH ключи не имеют срока годности.** Они работают до тех пор, пока:

1. ✅ Приватный ключ не удалён с твоего устройства
2. ✅ Публичный ключ не удалён из `~/.ssh/authorized_keys` на сервере
3. ✅ Сервер поддерживает алгоритм шифрования ключа (ED25519 поддерживается уже много лет)

**Бессрочно!** Твой текущий ключ (ED25519) будет работать столько, сколько тебе нужно.

---

### Когда ключ перестанет работать?

1. **Удаление ключа**
   - Если случайно удалишь приватный ключ с устройства
   - Если удалишь публичный ключ из `authorized_keys` на сервере

2. **Изменение прав доступа**
   - Если права на `~/.ssh/authorized_keys` станут неправильными (должно быть 600)
   - Если права на `~/.ssh/` станут неправильными (должно быть 700)

3. **Изменение конфигурации SSH**
   - Если отключишь `PubkeyAuthentication` в `/etc/ssh/sshd_config`
   - Если изменишь путь к `AuthorizedKeysFile`

---

### Что делать, если потерял доступ?

**Вариант 1: VPS панель управления**
- Если есть доступ к панели управления провайдера (например, Timeweb, Selectel и т.д.)
- Через веб-консоль можно подключиться к серверу
- Добавить новый ключ или временно включить пароль

**Вариант 2: Резервный SSH ключ**
- Храни резервную копию SSH ключа в безопасном месте (password manager)
- Или добавь несколько ключей (с разных устройств) перед отключением пароля

---

### Как восстановить ключи из бэкапа?

**Сценарий 1: Потерял приватный ключ на локальном устройстве**

```bash
# Mac/Linux
cd ~
# Если обычный архив:
tar -xzf ssh_backup_20251224.tar.gz
cp ssh_backup_20251224/id_ed25519 ~/.ssh/
cp ssh_backup_20251224/id_ed25519.pub ~/.ssh/

# Установи правильные права
chmod 600 ~/.ssh/id_ed25519
chmod 644 ~/.ssh/id_ed25519.pub
chmod 700 ~/.ssh
```

**Сценарий 2: Потерял доступ к серверу (удалил ключ из authorized_keys)**

1. Подключись через веб-консоль VPS провайдера
2. Восстанови `authorized_keys`:
```bash
# Если есть бэкап на сервере:
cp ~/.ssh/authorized_keys.backup_20251224 ~/.ssh/authorized_keys
chmod 600 ~/.ssh/authorized_keys
```

---

## 🔍 Объяснение сообщений SSH

### "The authenticity of host can't be established"

**Что происходит:**
Когда ты подключаешься к серверу по SSH/SCP в первый раз (или ключ хоста изменился), SSH показывает отпечаток ключа хоста и спрашивает подтверждение.

**Это нормально и безопасно!** Это защита от "Man-in-the-Middle" атак.

**Что делать:**
Просто введи `yes` и нажми Enter:
```bash
yes
```

SSH добавит ключ хоста в файл `~/.ssh/known_hosts` на твоём Mac, и больше не будет спрашивать при подключениях к этому серверу.

---

### Полезные команды

**Посмотреть все добавленные ключи:**
```bash
cat ~/.ssh/authorized_keys
```

**Удалить конкретный ключ:**
```bash
nano ~/.ssh/authorized_keys
# Удали строку с нужным ключом, сохрани (Ctrl+O, Enter, Ctrl+X)
```

**Проверить подключение перед отключением пароля:**
```bash
ssh -o PasswordAuthentication=no root@nowbots.ru
# Если подключается - ключ работает! ✅
# Если просит пароль - ключ не работает ❌
```

**Проверить отпечаток ключа:**
```bash
ssh-keygen -l -f ~/.ssh/id_ed25519.pub
```

---

## 🚨 Важные напоминания

1. **Приватный ключ = СЕКРЕТ!** Никогда не отправляй его по email, не коммить в Git, не загружай на публичные серверы
2. **Публичный ключ** можно показывать всем - он не секретный
3. **Храни бэкапы в безопасном месте** - зашифрованном облаке, password manager, или физически защищённом месте
4. **Регулярно проверяй бэкапы** - убедись, что можешь восстановить доступ
5. **Имей несколько ключей** - не храни все яйца в одной корзине

---

## ✅ Итоговая инструкция

**Прямо сейчас:**

1. **На локальном устройстве:**
   ```bash
   cd ~/.ssh
   tar -czf ~/ssh_backup_$(date +%Y%m%d).tar.gz id_ed25519 id_ed25519.pub
   # Сохрани в password manager или зашифрованное хранилище
   ```

2. **На сервере (подключись по SSH):**
   ```bash
   cp ~/.ssh/authorized_keys ~/.ssh/authorized_keys.backup_$(date +%Y%m%d)
   ```

3. **Скачай authorized_keys на локальное устройство:**
   ```bash
   # Выполни с локального устройства:
   scp root@nowbots.ru:/root/.ssh/authorized_keys.backup_* ~/Downloads/
   ```

4. **Проверь отпечаток ключа (для справки):**
   ```bash
   ssh-keygen -l -f ~/.ssh/id_ed25519.pub
   ```

Готово! Теперь у тебя есть надёжные бэкапы! 🎉

