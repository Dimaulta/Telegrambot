# 💾 Полное руководство по SSH ключам: формат, расположение и бэкап

## 📁 Где хранятся SSH ключи

### 🖥️ На твоём локальном устройстве (Mac/Windows/Linux)

**Стандартная директория:** `~/.ssh/` (в домашней папке)

**Пример полного пути:**
- Mac/Linux: `/Users/твоё_имя/.ssh/` или `/home/твоё_имя/.ssh/`
- Windows: `C:\Users\твоё_имя\.ssh\` (в PowerShell/Git Bash)

**Файлы в директории:**
```
~/.ssh/
├── id_ed25519          ← ПРИВАТНЫЙ ключ (СЕКРЕТНЫЙ! Никому не показывай!)
├── id_ed25519.pub      ← ПУБЛИЧНЫЙ ключ (можно показывать)
├── known_hosts         ← Список известных серверов
└── config              ← Конфигурация SSH (опционально)
```

### 🖧 На сервере (VPS)

**Директория:** `/root/.ssh/`

**Файлы:**
```
/root/.ssh/
├── authorized_keys     ← ПУБЛИЧНЫЕ ключи всех устройств, которым разрешён доступ
├── known_hosts         ← Список известных серверов
├── config              ← Конфигурация SSH (опционально)
└── github_vps          ← Другие ключи (например, для GitHub)
```

**ВАЖНО:** На сервере хранятся только ПУБЛИЧНЫЕ ключи в `authorized_keys`. Приватные ключи на сервере НЕ хранятся!

---

## 📄 Формат файлов SSH ключей

### 🔐 Приватный ключ (id_ed25519)

**Формат:** Текстовый файл, начинается с заголовка

**Пример начала файла:**
```
-----BEGIN OPENSSH PRIVATE KEY-----
b3BlbnNzaC1rZXktdjEAAAAABG5vbmUAAAAEbm9uZQAAAAAAAAABAAAAMwAAAAtz
c2gtZWQyNTUxOQAAACDLb3JBQ1pKNC9lQVg5VUdNcWlTTUkrckViRElpTHFVUHp3
...
(много строк закодированных данных)
...
-----END OPENSSH PRIVATE KEY-----
```

**Характеристики:**
- Начинается с `-----BEGIN OPENSSH PRIVATE KEY-----`
- Заканчивается на `-----END OPENSSH PRIVATE KEY-----`
- Содержит закодированные данные между этими маркерами
- **ЭТО СЕКРЕТНЫЙ ФАЙЛ!** Никогда не отправляй его никуда!

### 🔓 Публичный ключ (id_ed25519.pub или в authorized_keys)

**Формат:** Одна строка текста

**Пример:**
```
ssh-ed25519 AAAAC3NzaC1lZDI1NTE5AAAAILOXJBZJ4/eAX9UGMqiSMI+rEbDIiLqUPzw6TfHt2wcT lightpaintru@gmail.com
```

**Структура:**
- `ssh-ed25519` - тип алгоритма
- `AAAAC3NzaC1lZDI1NTE5AAAAI...` - сам ключ (закодирован в base64)
- `lightpaintru@gmail.com` - комментарий (обычно email или название устройства)

**В файле authorized_keys:**
- Каждая строка = один публичный ключ
- Можно несколько ключей (по одному на строку)
- Комментарии после ключа опциональны

---

## 💾 Как сделать бэкап ключей

### 🖥️ На локальном устройстве (где хранится приватный ключ)

#### Способ 1: Простое копирование файлов

**Mac/Linux:**
```bash
# Перейди в директорию с ключами
cd ~/.ssh

# Создай папку для бэкапа
mkdir -p ~/ssh_backup_$(date +%Y%m%d)

# Скопируй все ключи
cp id_ed25519 ~/ssh_backup_$(date +%Y%m%d)/
cp id_ed25519.pub ~/ssh_backup_$(date +%Y%m%d)/
cp config ~/ssh_backup_$(date +%Y%m%d)/ 2>/dev/null || true

# Создай архив
cd ~
tar -czf ssh_backup_$(date +%Y%m%d).tar.gz ssh_backup_$(date +%Y%m%d)/

# Удали временную папку
rm -rf ssh_backup_$(date +%Y%m%d)/
```

**Windows (PowerShell):**
```powershell
# Создай папку для бэкапа
$backupDir = "$env:USERPROFILE\ssh_backup_$(Get-Date -Format 'yyyyMMdd')"
New-Item -ItemType Directory -Path $backupDir -Force

# Скопируй ключи
Copy-Item "$env:USERPROFILE\.ssh\id_ed25519" -Destination $backupDir
Copy-Item "$env:USERPROFILE\.ssh\id_ed25519.pub" -Destination $backupDir
Copy-Item "$env:USERPROFILE\.ssh\config" -Destination $backupDir -ErrorAction SilentlyContinue
```

#### Способ 2: Зашифрованный архив

**Mac/Linux:**
```bash
# Создай зашифрованный архив (попросит ввести пароль)
cd ~/.ssh
tar -czf - id_ed25519 id_ed25519.pub config 2>/dev/null | \
    openssl enc -aes-256-cbc -salt -out ~/ssh_backup_encrypted_$(date +%Y%m%d).tar.gz.enc
```

**Расшифровка позже:**
```bash
openssl enc -d -aes-256-cbc -in ~/ssh_backup_encrypted_20251224.tar.gz.enc | \
    tar -xzf - -C ~/.ssh/
```

#### Способ 3: Сохранение в Password Manager

**Рекомендуемые инструменты:**
- 1Password
- LastPass
- Bitwarden
- KeePass

**Что сохранять:**
1. Приватный ключ (`id_ed25519`) - как секретная заметка
2. Публичный ключ (`id_ed25519.pub`) - тоже можно сохранить
3. Отпечаток ключа (fingerprint) - для идентификации

**Получить отпечаток:**
```bash
ssh-keygen -l -f ~/.ssh/id_ed25519.pub
```

---

### 🖧 На сервере (бэкап authorized_keys)

**ВАЖНО:** На сервере хранятся только публичные ключи, но их тоже нужно бэкапить!

```bash
# Подключись к серверу
ssh root@nowbots.ru

# Создай бэкап
cp ~/.ssh/authorized_keys ~/.ssh/authorized_keys.backup_$(date +%Y%m%d)

# Или сделай бэкап всей директории
tar -czf ~/ssh_server_backup_$(date +%Y%m%d).tar.gz ~/.ssh/

# Скопируй на локальное устройство (для надёжности)
# С локального устройства:
scp root@nowbots.ru:/root/ssh_server_backup_*.tar.gz ~/Downloads/
```

---

## 🔄 Как восстановить ключи из бэкапа

### Сценарий 1: Потерял приватный ключ на локальном устройстве

**Шаг 1: Найдите бэкап**
- Проверь папку с бэкапами
- Или скачай из password manager

**Шаг 2: Восстанови файлы**
```bash
# Mac/Linux
cd ~
# Если обычный архив:
tar -xzf ssh_backup_20251224.tar.gz
cp ssh_backup_20251224/id_ed25519 ~/.ssh/
cp ssh_backup_20251224/id_ed25519.pub ~/.ssh/

# Если зашифрованный:
openssl enc -d -aes-256-cbc -in ssh_backup_encrypted_20251224.tar.gz.enc | \
    tar -xzf - -C ~/.ssh/

# Установи правильные права
chmod 600 ~/.ssh/id_ed25519
chmod 644 ~/.ssh/id_ed25519.pub
chmod 700 ~/.ssh
```

**Windows (PowerShell):**
```powershell
# Распакуй архив
Expand-Archive -Path "$env:USERPROFILE\Downloads\ssh_backup_20251224.zip" -DestinationPath "$env:TEMP"

# Скопируй файлы
Copy-Item "$env:TEMP\ssh_backup_20251224\id_ed25519" -Destination "$env:USERPROFILE\.ssh\"
Copy-Item "$env:TEMP\ssh_backup_20251224\id_ed25519.pub" -Destination "$env:USERPROFILE\.ssh\"

# Установи права (требует PowerShell от администратора)
icacls "$env:USERPROFILE\.ssh\id_ed25519" /inheritance:r /grant:r "${env:USERNAME}:F"
```

**Шаг 3: Проверь подключение**
```bash
ssh root@nowbots.ru
# Должно подключиться без пароля!
```

---

### Сценарий 2: Потерял доступ к серверу (удалил ключ из authorized_keys)

**Вариант А: Есть доступ через веб-консоль панели управления**

1. Подключись через веб-консоль VPS провайдера
2. Восстанови `authorized_keys`:
```bash
# Если есть бэкап на сервере:
cp ~/.ssh/authorized_keys.backup_20251224 ~/.ssh/authorized_keys
chmod 600 ~/.ssh/authorized_keys

# Или добавь ключ заново:
echo "твой_публичный_ключ_здесь" >> ~/.ssh/authorized_keys
chmod 600 ~/.ssh/authorized_keys
```

**Вариант Б: Восстановил из бэкапа сервера**

Если ты делал бэкап `authorized_keys` локально:
```bash
# С локального устройства (если есть другой способ доступа):
scp authorized_keys.backup root@nowbots.ru:/root/.ssh/authorized_keys

# Или через веб-консоль:
# 1. Распакуй бэкап локально
# 2. Скопируй содержимое authorized_keys
# 3. Вставь через веб-консоль в файл /root/.ssh/authorized_keys
```

---

### Сценарий 3: Новое устройство - нужно подключиться

**Если есть бэкап ключа:**

1. Скопируй приватный ключ на новое устройство (из бэкапа)
2. Установи правильные права:
   ```bash
   chmod 600 ~/.ssh/id_ed25519
   chmod 644 ~/.ssh/id_ed25519.pub
   ```
3. Подключись:
   ```bash
   ssh -i ~/.ssh/id_ed25519 root@nowbots.ru
   ```

**Если бэкапа нет, но ключ есть на старом устройстве:**

1. На старом устройстве скопируй публичный ключ:
   ```bash
   cat ~/.ssh/id_ed25519.pub
   ```
2. Подключись к серверу со старого устройства
3. Добавь публичный ключ нового устройства в `authorized_keys`:
   ```bash
   echo "публичный_ключ_нового_устройства" >> ~/.ssh/authorized_keys
   ```
4. Теперь можешь подключиться с нового устройства

---

## 📋 Чеклист для бэкапа

### ✅ Регулярные действия:

- [ ] Сделай бэкап приватного ключа после создания
- [ ] Сохрани бэкап в 2-3 местах (локально, облако, password manager)
- [ ] Периодически обновляй бэкапы (раз в месяц/квартал)
- [ ] Проверяй, что бэкапы актуальны

### ✅ Что бэкапить:

**Локально:**
- [ ] Приватный ключ (`~/.ssh/id_ed25519`)
- [ ] Публичный ключ (`~/.ssh/id_ed25519.pub`)
- [ ] SSH config (`~/.ssh/config`) - если есть
- [ ] Отпечатки ключей (fingerprint)

**На сервере:**
- [ ] Файл `authorized_keys` (`/root/.ssh/authorized_keys`)
- [ ] Вся директория `.ssh` (для надёжности)

---

## 🔍 Проверка целостности ключей

**Проверить, что ключи не повреждены:**

```bash
# Локально:
ssh-keygen -l -f ~/.ssh/id_ed25519.pub
# Должен показать отпечаток, например:
# 256 SHA256:sSLA0kiAEU/Yd/wnsl0439oLnH1c/QmoJfTEydl8Ovc (ED25519)

# На сервере:
ssh-keygen -l -f ~/.ssh/authorized_keys
# Покажет отпечатки всех ключей
```

**Проверить, что приватный и публичный ключ соответствуют друг другу:**

```bash
# Локально:
ssh-keygen -y -f ~/.ssh/id_ed25519
# Выведет публичный ключ, который должен совпадать с содержимым id_ed25519.pub
```

---

## 💡 Практические примеры

### Пример 1: Полный бэкап на Mac

```bash
# 1. Создай бэкап
cd ~/.ssh
tar -czf ~/Documents/ssh_backup_$(date +%Y%m%d).tar.gz id_ed25519 id_ed25519.pub config

# 2. Скопируй на внешний диск (для надёжности)
cp ~/Documents/ssh_backup_*.tar.gz /Volumes/ExternalDisk/Backups/

# 3. Сохрани в iCloud (если используешь)
cp ~/Documents/ssh_backup_*.tar.gz ~/Library/Mobile\ Documents/com~apple~CloudDocs/Backups/
```

### Пример 2: Бэкап в password manager (1Password)

1. Открой 1Password
2. Создай новую Secure Note
3. Название: "SSH Key VPS Server - 2025"
4. В содержимое вставь:
   - Приватный ключ (из `~/.ssh/id_ed25519`)
   - Публичный ключ (из `~/.ssh/id_ed25519.pub`)
   - Отпечаток (fingerprint)
   - Дата создания бэкапа

### Пример 3: Бэкап authorized_keys с сервера

```bash
# На сервере:
cat ~/.ssh/authorized_keys > ~/authorized_keys_backup_$(date +%Y%m%d).txt

# Скопируй на локальное устройство:
# С локального устройства:
scp root@nowbots.ru:/root/authorized_keys_backup_*.txt ~/Downloads/
```

---

## 🚨 Важные напоминания

1. **Приватный ключ = СЕКРЕТ!** Никогда не отправляй его по email, не коммить в Git, не загружай на публичные серверы
2. **Публичный ключ** можно показывать всем - он не секретный
3. **Храни бэкапы в безопасном месте** - зашифрованном облаке, password manager, или физически защищённом месте
4. **Регулярно проверяй бэкапы** - убедись, что можешь восстановить доступ
5. **Имей несколько ключей** - не храни все яйца в одной корзине

---

## ✅ Итоговая инструкция для тебя

**Прямо сейчас:**

1. **На локальном устройстве:**
   ```bash
   # Создай бэкап
   cd ~/.ssh
   tar -czf ~/ssh_backup_$(date +%Y%m%d).tar.gz id_ed25519 id_ed25519.pub
   
   # Сохрани в безопасное место (password manager, облако, внешний диск)
   ```

2. **На сервере:**
   ```bash
   # Создай бэкап authorized_keys
   cp ~/.ssh/authorized_keys ~/.ssh/authorized_keys.backup_$(date +%Y%m%d)
   
   # Скопируй на локальное устройство (для надёжности)
   # С локального устройства:
   scp root@nowbots.ru:/root/.ssh/authorized_keys.backup_* ~/Downloads/
   ```

3. **Проверь отпечаток ключа:**
   ```bash
   # Запиши куда-нибудь для справки
   ssh-keygen -l -f ~/.ssh/id_ed25519.pub
   ```

Готово! Теперь у тебя есть надёжные бэкапы! 🎉

