# 🔐 Объяснение: "The authenticity of host can't be established"

## 📋 Что происходит

Когда ты подключаешься к серверу по SSH/SCP в первый раз (или ключ хоста изменился), SSH показывает отпечаток ключа хоста и спрашивает подтверждение.

**Это нормально и безопасно!** Это защита от "Man-in-the-Middle" атак.

## 🔍 Что показывает сообщение

```
The authenticity of host 'nowbots.ru (85.208.110.226)' can't be established.
ED25519 key fingerprint is SHA256:kZqyf+DE2kmvc1SSt/PetVr4v5umgGc4dWW20AWbvzU.
```

**Объяснение:**
- `nowbots.ru (85.208.110.226)` - домен и IP адрес сервера
- `ED25519 key fingerprint` - отпечаток ключа хоста сервера
- SSH не находит запись для домена `nowbots.ru`, но находит для IP `85.208.110.226`

## ✅ Что делать

**Просто введи `yes` и нажми Enter:**

```bash
yes
```

SSH добавит ключ хоста в файл `~/.ssh/known_hosts` на твоём Mac, и больше не будет спрашивать при подключениях к этому серверу.

## 🔒 Безопасность

**Проверь отпечаток ключа:**
- Текущий отпечаток: `SHA256:kZqyf+DE2kmvc1SSt/PetVr4v5umgGc4dWW20AWbvzU`
- Если отпечаток совпадает - всё в порядке, вводи `yes`
- Если отпечаток не совпадает - возможно, что-то не так (но в твоём случае это нормально, так как подключаешься по домену впервые)

## 📝 Что происходит дальше

После ввода `yes`:
1. SSH сохранит ключ хоста в `~/.ssh/known_hosts`
2. Команда `scp` продолжит выполнение
3. Файл `authorized_keys.backup_20251224` будет скопирован в `~/Downloads/`

## 💡 Альтернатива (если не хочешь подтверждать каждый раз)

Можно добавить ключ хоста автоматически:
```bash
ssh-keyscan -H nowbots.ru >> ~/.ssh/known_hosts
```

Но в твоём случае просто введи `yes` - это проще и безопаснее!

---

**Итог:** Это нормальное поведение SSH. Просто введи `yes` и продолжай! ✅


