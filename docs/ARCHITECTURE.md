# Архитектура проекта




## Обзор системы

Проект представляет собой микросервисную архитектуру для Telegram ботов с возможностью обработки видео и веб-интерфейсом.

### Статус разработки

- ✅ **MVP (7 ботов):**
  - `nowcontrollerbot` — управление отправкой сообщений в боты NowBots и включение-выключение проверки на подписку каналов-спонсоров
  - `Roundsvideobot` — обработка видео в видеокружки с miniapp интерфейсом
  - `filenowbot` — скачивание TikTok видео без водяного знака
  - `golosnowbot` — голос → текст через SaluteSpeech (ранее gsfortextbot)
  - `contentfabrikabot` — генерация постов в стиле автора через OpenAI
  - `Neurfotobot` — обработка изображений через AI, нейрофото с лицом пользователя
  - `pereskaznowbot` — получение расшифровки и саммари YouTube видео через OpenAI Whisper API и GPT

- 🛠️ **В разработке (2 бота):**
  - `NeurVideoBot` — генерация видео сервисами OpenAI, Google и другими, MVP ещё в работе
  - `AntispamNowBot` — антиспам бот: выключатель на ночь, капча для вступления, запрещатель сообщений от каналов, реализован каркас







## Компоненты системы

### 1. Core Server (Порт 8080)
**Назначение:** Основной сервер, точка входа для всех запросов

**Функции:**
- Проксирование webhook'ов к video-processing
- CORS поддержка для miniapp
- Статическая раздача файлов
- База данных SQLite для метаданных

**Файлы:**
- `core-server/Sources/App/routes.swift` — маршруты и прокси
- `core-server/Sources/App/configure.swift` — конфигурация сервера

### 2. Video Processing Service (Порт 8081)
**Назначение:** Обработка видео и miniapp интерфейс

**Функции:**
- Telegram miniapp для выбора области кропа
- Обработка видео через FFmpeg
- Создание видеокружков
- Прямая обработка видео из чата

**Файлы:**
- `Roundsvideobot/VideoService/Internal/VideoProcessor.swift` — обработка видео
- `Roundsvideobot/VideoService/Public/` — фронтенд miniapp
- `Roundsvideobot/VideoService/Internal/routes.swift` — API endpoints

### 3. Bot Services

#### 3.1. FileNowBot (Порт 8085) ✅ **MVP**
**Статус:** Доведен до MVP, работает в продакшене

**Назначение:** Бот для скачивания TikTok видео без водяного знака

**Функции:**
- Извлечение прямых ссылок на TikTok видео
- Отправка видео пользователю через Telegram API
- Обработка различных форматов TikTok URL (vm.tiktok.com, tiktok.com, vt.tiktok.com)

**Файлы:**
- `filenowbot/Sources/App/Controllers/FileNowBotController.swift` — основной контроллер
- `filenowbot/Sources/App/Internal/TikTokResolver.swift` — резолвер TikTok ссылок
- `filenowbot/Sources/App/Middleware/LoggingMiddleware.swift` — логирование

#### 3.2. GolosNowBot (Порт 8083) ✅ **MVP**
**Статус:** Доведен до MVP, работает в продакшене (ранее gsfortextbot)

**Назначение:** Расшифровка голосовых и аудиосообщений в текст через SaluteSpeech

**Функции:**
- `SaluteSpeechAuthService` автоматически обновляет OAuth-токен (учитывает `SALUTESPEECH_TOKEN_EPSILON_SECONDS`)
- `SaluteSpeechRecognitionService` отправляет аудио на распознавание и возвращает текст
- Контроллер обрабатывает `/start`, голосовые, документы с аудио и отправляет ответы в чат

**Документация:** `golosnowbot/` — ключи SaluteSpeech (`GOLOSNOWBOT_TOKEN`, `SALUTESPEECH_*`), сертификаты в `config/certs/salutespeech-chain.pem`.

#### 3.3. ContentFabrikaBot (Порт 8089) ✅ **MVP**
**Статус:** Доведен до MVP, работает в продакшене

**Назначение:** Генерация постов для Telegram каналов в уникальном стиле автора через OpenAI API

**Функции:**
- Автоматическое сохранение постов из канала в базу данных SQLite
- Анализ стиля написания автора на основе последних 10 постов через OpenAI (`gpt-4o-mini`)
- Генерация новых постов по заданной теме в стиле автора
- Публикация постов на канал (отложенные публикации в разработке)
- Поддержка нескольких каналов для одного пользователя
- Переобучение стиля через команду `/relearn`
- Команда `/reset` для удаления всех данных пользователя

**База данных:**
- `channels` — информация о каналах (ID канала, владелец, название)
- `style_profiles` — профили стиля авторов (описание стиля, количество проанализированных постов)
- `channel_posts` — сохраненные посты из каналов (текст, дата, message_id)

**Файлы:**
- `contentfabrikabot/Sources/App/Controllers/ContentFabrikaBotController.swift` — основной контроллер
- `contentfabrikabot/Sources/App/Services/OpenAIStyleService.swift` — сервис для работы с OpenAI API
- `contentfabrikabot/Sources/App/Services/TelegramChannelService.swift` — работа с каналами Telegram
- `contentfabrikabot/Sources/App/Services/KeyboardService.swift` — централизация клавиатур бота
- `contentfabrikabot/Sources/App/Services/PostGenerationService.swift` — генерация и публикация постов
- `contentfabrikabot/Sources/App/Middleware/LoggingMiddleware.swift` — логирование для диагностики
- `contentfabrikabot/db.sqlite` — база данных SQLite (создается автоматически)

**Особенности Telegram API:**
- Бот должен быть добавлен как администратор канала с правом публикации
- Telegram Bot API не позволяет получать историю сообщений канала напрямую
- Бот сохраняет только те посты, которые получил после того, как стал админом канала
- Автоматическое ограничение количества сохраненных постов (максимум 50 последних)
- Обработка webhook'ов: `message`, `callback_query`, `channel_post`, `my_chat_member`
- Поддержка постов с фото (placeholder из Unsplash)
- Проверка длины caption (максимум 1024 символа)
- **TODO**: Отладка публикации поста — необходимо создавать в отложенные публикации, а не сразу на канал

#### 3.4. NeurVideoBot (Порт 8086) 🛠️ **В разработке**
**Статус:** Проект в активной разработке, MVP ещё не выпущен

**Назначение:** Генерация видео сервисами OpenAI, Google и другими

**Текущий прогресс:**
- Конфигурация подгружает `config/.env`, поддерживает HTTP-прокси
- Сервисы формируют запросы к различным видео-генераторам
- Маршруты и контроллеры принимают промпты, требуется реализация выдачи результата пользователю

**Следующие шаги:** очередь задач, отслеживание статусов и отправка готовых видео в Telegram.

#### 3.5. AntispamNowBot (Порт 8088) 🛠️ **В разработке**
**Статус:** Каркас сервиса готов, функционал антиспама ещё не реализован

**Назначение:** Антиспам бот: выключатель на ночь, капча для вступления, запрещатель сообщений от каналов

**Текущий прогресс:**
- Контроллер обрабатывает команды управления
- Реализована базовая структура для антиспам функций
- Требуется реализация капчи, ночного режима и фильтрации сообщений от каналов

**Следующие шаги:** реализовать функционал антиспама и интеграцию с Telegram API.

#### 3.6. Neurfotobot (Порт 8082) ✅ **MVP**
**Статус:** Бот полностью готов, работает в продакшене

**Назначение:** Бот для нейрофотографий (AI обработка изображений)

**Функции:**
- Обработка изображений через Replicate API
- Автоматический перевод промптов с русского на английский через Yandex Translate API
- Модерация контента через Google Vision API
- Локальное хранение фото и датасетов (через NEURFOTOBOT_TEMP_DIR)
- Генерация нейрофото с лицом пользователя по обученной модели

#### 3.7. PereskazNowBot (Порт 8090) ✅ **MVP**
**Статус:** Бот готов, работает в продакшене

**Назначение:** Получение расшифровки и саммари YouTube видео через OpenAI Whisper API и GPT

**Функции:**
- Обработка YouTube ссылок различных форматов
- Скачивание аудио через `yt-dlp`
- Расшифровка аудио через OpenAI Whisper API
- Генерация саммари через OpenAI GPT
- Разбиение длинных сообщений на части (лимит Telegram 4096 символов)
- Rate limiting: не более 1 ссылки в 2 минуты, максимум 20 видео в день на пользователя
- Максимальная длительность видео: 30 минут

**Файлы:**
- `pereskaznowbot/Sources/App/Controllers/PereskazNowBotController.swift` — основной контроллер
- `pereskaznowbot/Sources/App/Services/PereskazService.swift` — сервис для работы с OpenAI API
- `pereskaznowbot/Sources/App/Internal/RateLimiter.swift` — rate limiting

**Требования:**
- `PERESKAZNOWBOT_TOKEN` — токен бота от @BotFather
- `PERESKAZ_OPENAI_SERVICE` — OpenAI API ключ для Whisper API и GPT
- `yt-dlp` — установлен через `brew install yt-dlp`
- `ffmpeg` — установлен через `brew install ffmpeg` (для сжатия больших аудио файлов)

#### 3.8. NowControllerBot (Порт 8084) ✅ **MVP**
**Статус:** Бот готов и используется для управления отправкой сообщений в боты NowBots

**Назначение:** Управление отправкой сообщений в боты NowBots и включение-выключение проверки на подписку каналов-спонсоров





## Потоки данных

### Обработка видео через miniapp (Roundsvideobot)
```
Пользователь → miniapp → /api/upload → VideoProcessor → Telegram API
```

#### Как работают мини-аппы в Telegram и кэш

Мини-апп в Telegram — это обычная веб-страница (HTML + JS + CSS), которая открывается по URL в **WebView** внутри клиента Telegram. Сервер отдаёт `index.html`, в нём подключаются `app.js` и `styles.css`. Браузерный движок (WebView) **кэширует** эти файлы по URL: если адрес не меняется, при следующем открытии мини-аппа может подставиться старая версия из кэша.

**Важно:** после деплоя код на сервере новый, но пользователи (и ты с телефона) можем продолжать видеть **старое** поведение — например, избыточное приближение в кружке — пока не обновится кэш. На Mac при локальной разработке кэш часто другой или его нет, поэтому локально всё ок, а в проде в Telegram — «как раньше».

**Что делать:** при любых правках в `app.js` или `styles.css` нужно **увеличить** параметр `?v=` в `Roundsvideobot/VideoService/Public/index.html` (например, `app.js?v=10` → `app.js?v=11`). Новый URL заставляет клиента запросить файл заново, кэш по старому URL не используется. Иначе после мержа и деплоя часть пользователей будет получать старый JS из кэша.

**Проверка после деплоя:** в мини-аппе сверху справа → **три точки (⋮)** → **«Обновить страницу»** — кэш сбрасывается, сразу видно актуальную версию. Либо закрыть и заново открыть веб-апп.

Подробнее и чеклист для агента на VPS: [`docs/archive/ROUNDSVIDEOBOT_MINIAPP_CACHE_FIX.md`](archive/ROUNDSVIDEOBOT_MINIAPP_CACHE_FIX.md).

### Прямая обработка видео (Roundsvideobot)
```
Пользователь → Telegram → /webhook → VideoProcessor → Telegram API
```

### Проксирование через core-server (Roundsvideobot)
```
Telegram → core-server/webhook → video-processing/webhook
```

### Обработка TikTok видео (filenowbot)
```
Пользователь → Telegram → /webhook → FileNowBotController → TikTokResolver → Telegram API
```

**Процесс:**
1. Пользователь отправляет TikTok ссылку боту
2. Контроллер извлекает URL из сообщения
3. TikTokResolver получает прямую ссылку на видео
4. Видео отправляется пользователю через Telegram API

### Обработка YouTube видео (pereskaznowbot) ✅
```
Пользователь → Telegram → /webhook → PereskazNowBotController → PereskazService → yt-dlp → OpenAI Whisper API → OpenAI GPT → Telegram API
```

**Процесс:**
1. Пользователь отправляет YouTube ссылку боту
2. Контроллер извлекает URL из сообщения и проверяет rate limit
3. PereskazService скачивает аудио через `yt-dlp`
4. Аудио отправляется на расшифровку через OpenAI Whisper API
5. Расшифровка отправляется на генерацию саммари через OpenAI GPT
6. Саммари отправляется пользователю через Telegram API (с разбиением на части, если длинное)





## Конфигурация

### Переменные окружения

**Обязательные (для работающих ботов):**
- `BASE_URL` — базовый URL для webhook'ов
- `VIDEO_BOT_TOKEN` — Roundsvideobot
- `FILENOWBOT_TOKEN` — FileNowBot
- `GOLOSNOWBOT_TOKEN`, `SALUTESPEECH_AUTH_KEY` — GolosNowBot (голос → текст, SaluteSpeech)
- `CONTENTFABRIKABOT_TOKEN`, `OPENAI_API_KEY` — ContentFabrikaBot

**Дополнительные (для новых/замороженных сервисов):**
- `PERESKAZNOWBOT_TOKEN`, `PERESKAZ_OPENAI_SERVICE` — PereskazNowBot ✅ (MVP)
- `ANTISPAMNOWBOT_TOKEN`, `ANTISPAMNOWBOT_PORT` — AntispamNowBot 🛠️
- `NEURVIDEOBOT_TOKEN`, `NEURVIDEOBOT_API_URL`, `NEURVIDEOBOT_API_KEY`, `NEURVIDEOBOT_HTTP_PROXY`, `NEURVIDEOBOT_PROXY_*` — NeurVideoBot 🛠️
- `NEURFOTOBOT_TOKEN` и связанные ключи (Replicate, Yandex Translate, Google Vision, OpenAI) — Neurfotobot ✅
- `NOWCONTROLLERBOT_TOKEN` — NowControllerBot (MVP)

### Файлы конфигурации
- `config/server.json` — настройки сервера
- `config/services.json` — конфигурация сервисов
- `config/.env` — переменные окружения (создается пользователем)





## Технологический стек

- **Backend:** Swift + Vapor
- **Frontend:** HTML/CSS/JavaScript + Telegram Web App API
- **Обработка видео:** FFmpeg
- **База данных:** SQLite
- **Протокол:** HTTP/HTTPS
- **Контейнеризация:** Docker, Docker Compose





## Масштабирование

### Горизонтальное масштабирование
- Каждый сервис может быть развернут на отдельном сервере
- Использование load balancer для распределения нагрузки
- Независимое масштабирование компонентов

### Вертикальное масштабирование
- Увеличение ресурсов для обработки видео
- Оптимизация FFmpeg параметров
- Кэширование статических ресурсов





## Безопасность

### Аутентификация
- Токены ботов через переменные окружения
- Валидация webhook'ов от Telegram
- CORS настройки для miniapp

### Защита данных
- Временные файлы автоматически удаляются
- Ограничения на размер и длительность видео
- Валидация входных данных





## Мониторинг и логирование

### Логирование
- Структурированные логи через Swift Logging
- Отслеживание ошибок обработки
- Мониторинг производительности FFmpeg

### Метрики
- Время обработки видео
- Количество обработанных файлов
- Статистика ошибок

