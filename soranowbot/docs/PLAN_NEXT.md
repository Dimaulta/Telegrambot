# План работы на завтра - Устранение проблем с ватермаркой

## 📋 Краткая сводка
**Проблема:** Бот возвращает не то видео и видео с ватермаркой  
**Что сделано:** 
- ✅ Оптимизирована скорость работы (уменьшены таймауты, параллельные запросы)
- ✅ Добавлена проверка URL перед отправкой (HEAD запрос)
- ✅ Добавлен fallback: если URL не работает, скачиваем видео и отправляем файл
**Что нужно:** Найти правильный UUID без ватермарки через токен сессии

## Текущая ситуация
- ✅ Бот работает быстрее (оптимизированы таймауты и параллельные запросы)
- ✅ Добавлена проверка URL и fallback на скачивание видео
- ❌ Бот возвращает **не то видео** из аккаунта
- ❌ Видео всё ещё **с ватермаркой**
- ⚠️ Токен сессии может быть неправильного формата (начался с `token=`)
- ⚠️ **НОВАЯ ПРОБЛЕМА:** URL из Azure Storage возвращает ошибку "Bad or missing ac" (неправильно сконструированная ссылка)
- ✅ **ИСПРАВЛЕНО:** Убрана логика конструирования ссылки (SAS токены специфичны для каждого файла)
- ✅ **ИСПРАВЛЕНО:** Улучшена проверка URL - теперь проверяем тело ответа на ошибки Azure Storage

## Проблемы, которые нужно решить

### 1. Проблема с токеном сессии
**Симптом:** Токен начинался с `token=`, возможно это не токен сессии, а кука или другой формат.

**Что сделать:**
- [ ] Проверить логи - какие UUID находятся в HTML
- [ ] Проверить логи - какие UUID находятся в API ответах
- [ ] Убедиться, что токен сессии правильно используется в запросах
- [ ] Проверить, нужно ли использовать токен в заголовке `Authorization: Bearer` вместо куки

### 2. Выбор неправильного UUID
**Симптом:** Бот возвращает не то видео и видео с ватермаркой.

**Что сделать:**
- [ ] Проверить логи - какие UUID находятся и какой из них выбирается
- [ ] Улучшить логику выбора UUID - выбирать UUID БЕЗ ватермарки
- [ ] Проверить, что UUID из `__NEXT_DATA__` правильно извлекается
- [ ] Убедиться, что выбирается UUID из `videoUrls` без ватермарки, а не из `thumbnailUrls`

### 3. API endpoints не работают
**Симптом:** Все API endpoints возвращают 404.

**Что сделать:**
- [ ] Проверить логи - что возвращают `/api/auth/session` и другие endpoints
- [ ] Найти правильные API endpoints, которые работают с токеном сессии
- [ ] Возможно, использовать GraphQL API вместо REST
- [ ] Проверить, нужны ли другие заголовки для API запросов

### 4. Ошибка аутентификации Azure Storage (НОВАЯ!)
**Симптом:** URL из Azure Storage возвращает ошибку `AuthenticationFailed: Signature fields not well formed`.

**Что сделать:**
- [ ] Проверить, что URL правильно извлекается (не обрезается токен)
- [ ] Проверить, что токен в URL не истёк
- [ ] Убедиться, что URL используется сразу после извлечения (токены могут быть временными)
- [ ] Если URL не работает, использовать fallback: скачать видео и отправить файл (уже реализовано)
- [ ] Проверить логи - какой именно URL был выбран и почему он не работает

## Шаги для решения

### Шаг 1: Анализ логов (ПЕРВОЕ ДЕЛО С УТРА!)
```bash
# Посмотреть последние логи после отправки ссылки
docker logs playwright-service --tail 500

# Найти все UUID, которые находятся
docker logs playwright-service | grep -E "UUID|uuid" | tail -50

# Проверить, что возвращают API endpoints
docker logs playwright-service | grep -E "API request.*\[200\]|Получен ответ от API" | tail -20

# Проверить приоритизацию UUID
docker logs playwright-service | grep -E "Приоритизированные URL|MAIN VIDEO|NOT MAIN VIDEO" | tail -20

# Проверить, какой UUID выбран финально
docker logs playwright-service | grep -E "expectedUuid|Используем UUID" | tail -10
```

**Важные вопросы для ответа:**
1. Какие UUID находятся? (список всех найденных)
2. Какой UUID помечен как "MAIN VIDEO" (с ватермаркой)?
3. Какой UUID помечен как "NOT MAIN VIDEO" (без ватермарки)?
4. Какой UUID выбран финально?
5. Все ли UUID начинаются с `00000000-`?

### Шаг 2: Проверка токена
- [ ] Проверить формат токена в `.env` файле
- [ ] Убедиться, что токен используется правильно (кука vs заголовок Authorization)
- [ ] Попробовать использовать токен в заголовке `Authorization: Bearer <token>`

### Шаг 3: Улучшение логики выбора UUID
- [ ] Найти место в коде, где выбирается UUID
- [ ] Улучшить логику - выбирать UUID из `videoUrls` (без ватермарки), а не из `thumbnailUrls` (с ватермаркой)
- [ ] Добавить проверку - если UUID начинается с `00000000-`, то это UUID с ватермаркой, нужно искать другой

### Шаг 4: Тестирование
- [ ] Отправить ссылку в бота
- [ ] Проверить логи - какие UUID находятся
- [ ] Проверить, какое видео возвращается
- [ ] Убедиться, что видео без ватермарки

## Файлы для проверки
- `playwright-service/index.js` - основная логика извлечения UUID
- `config/.env` - токен сессии
- Логи контейнера - для анализа

## Ключевые места в коде
1. **Использование токена** (~строка 440-460) - где токен добавляется в куки
2. **API запросы** (~строка 505-590) - где делаются параллельные запросы к API
3. **Извлечение UUID из HTML** (~строка 885-930) - где ищутся UUID в HTML
4. **Выбор UUID** (~строка 947-980) - где происходит приоритизация и выбор финального UUID
5. **Финальный ответ** (~строка 994-1004) - где возвращается результат

## Возможные проблемы и решения

### Проблема 1: Все UUID начинаются с `00000000-`
**Решение:** 
- Нужно использовать токен сессии для получения данных через API
- Проверить, что токен правильно используется в заголовке `Authorization`
- Возможно, нужно использовать другой формат токена (Bearer вместо куки)

### Проблема 2: UUID без ватермарки не находится в HTML
**Решение:**
- Убедиться, что `__NEXT_DATA__` полностью загружен
- Проверить, что токен сессии даёт доступ к данным без ватермарки
- Возможно, нужно использовать GraphQL API вместо REST

### Проблема 3: Выбирается не тот UUID
**Решение:**
- Проверить логику приоритизации (строка 951-966)
- Убедиться, что `expectedUuid` правильно устанавливается
- Проверить, что `mainVideoUuids` правильно определяется

## Вопросы для выяснения
1. Какой формат у токена? (JWE, JWT, просто строка?)
2. Какие UUID находятся в HTML? (все ли начинаются с `00000000-`?)
3. Что возвращает `/api/auth/session`?
4. Есть ли в `__NEXT_DATA__` данные о видео без ватермарки?

## Примечания
- Токен сессии должен быть из DevTools → Application → Cookies → `__Secure-next-auth.session-token`
- UUID с ватермаркой обычно начинается с `00000000-`
- UUID без ватермарки имеет другой формат (например, `3d249c53-07fa-4ba4-9b65-0bf8eb4ea46a`)

